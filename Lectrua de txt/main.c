#include<stdio.h>

void FileWriteOpne(FILE **fp){
    //FILE *fp;
    
    // FILE *fp;
    // fp = fopen ("file.txt", "r");
    // char buffer2[1000] = {0};
    // fread(buffer2, sizeof(char), sizeof(buffer2), fp);
    // printf("El contenido es: %s", buffer2);
    // fclose(fp);


    // FILE *fp;
    // fp = fopen ("file.txt", "r");
    // char buffer[256];
    // while (fscanf(fp, "%255s", buffer) == 1)
    // {
    //     printf("%s\n", buffer);
    // }
    // fclose(fp);
    
    // fp = fopen("file.txt", "w+");


    // FILE *fp;
    // char str1[100];
    // char str2[100] = "\nPrueba de \nuna cadena";

    // printf("Ingresa el string: ");
    // scanf("%[^\n]", str1); // sigue leyendo hasta el salto de linea
    // fprintf(fp, "%s-%s", str1, str2);

    // FILE *fp;
    // fp = fopen("file.txt", "r");

    // char str1[100];

    // fscanf(fp, "%s", str1);
    // printf("String from file: %s\n", str1);

    // fclose(fp); 

    
}
